[core]
    remote = minio
['remote "minio_remote"']
    url = s3://dvc-data
    endpointurl = http://127.0.0.1:9000
    access_key_id = minioadmin
    secret_access_key = minioadmin
['remote "minio"']
    url = s3://dvc-store
    endpointurl = http://127.0.0.1:9000
    access_key_id = minioadmin
    secret_access_key = minioadmin
